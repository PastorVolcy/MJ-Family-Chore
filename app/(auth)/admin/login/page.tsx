import { adminLogin } from "@/app/actions";
export default async function Login({ searchParams }: { searchParams: Promise<{ error?: string }> }) { const { error } = await searchParams; return <main className="flex min-h-screen items-center justify-center bg-[#f8f5ed] p-5"><form action={adminLogin} className="w-full max-w-sm rounded-3xl bg-white p-7 shadow-xl"><p className="text-sm font-bold uppercase tracking-widest text-[#d59f11]">MJ Crew</p><h1 className="mt-2 text-3xl font-black text-[#145353]">Admin sign in</h1><p className="mt-2 text-sm text-stone-600">Enter the family admin PIN to manage chores.</p>{error && <p className="mt-4 rounded-lg bg-red-50 p-3 text-sm font-bold text-red-700">{error}</p>}<label className="label mt-6">PIN</label><input autoFocus required name="pin" type="password" inputMode="numeric" /><button className="btn-primary mt-5 w-full">Sign in</button></form></main>; }
