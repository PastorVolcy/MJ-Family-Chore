import { notFound } from "next/navigation";
import { prisma } from "@/lib/prisma";
import { itemSummary, money } from "@/lib/orders";
export default async function Confirmation({ params }: { params: Promise<{ id: string }> }) { const order = await prisma.order.findUnique({ where: { id: (await params).id } }); if (!order) notFound(); return <main className="flex min-h-screen items-center justify-center bg-cream p-5"><section className="card max-w-xl text-center"><p className="text-4xl">🎉</p><p className="mt-3 text-sm font-bold uppercase tracking-widest text-hibiscus">Order received</p><h1 className="mt-2 text-3xl font-black">Thank you, {order.firstName}!</h1><p className="mt-4 text-stone-600">Your preorder for <strong>{itemSummary(order)}</strong> is in. Your total due is <strong>{money(order.totalCents)}</strong>.</p><div className="mt-6 rounded-xl bg-stone-50 p-4 text-sm font-medium">Your order has been received. Pickup day will be confirmed by text.</div><p className="mt-6 text-sm text-stone-500">Need to update something? Call or text 973-207-1611.</p></section></main>; }
